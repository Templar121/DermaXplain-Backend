# Core tooling
setuptools>=65.5.0
wheel

# ASGI framework
fastapi
uvicorn[standard]

# ORM & DB
motor            # async MongoDB driver

# Auth & Security
passlib[bcrypt]
python-jose[cryptography]

# Validation
pydantic
email-validator

# Forms & file uploads
python-multipart

# Env vars
python-dotenv

# Image processing
Pillow

# ML runtime — CPU only
tensorflow-cpu==2.17.1

# Numeric library (keep on 1.x to avoid compatibility issues)
numpy<2.0,>=1.24.0

# SSL & DNS — REQUIRED for MongoDB Atlas on Render
certifi
dnspython
google-auth
google-auth-oauthlib
PyJWT